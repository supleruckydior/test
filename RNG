local player = game:GetService("Players").LocalPlayer
local gui = Instance.new("ScreenGui")
gui.Name = "BossCDMonitor"
gui.ResetOnSpawn = false

-- ========== GUI界面 ==========
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 280, 0, 350)
frame.Position = UDim2.new(0.5, -140, 0.5, -175)
frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
frame.Active = true
frame.Selectable = true
frame.Parent = gui

-- GUI拖动功能
local UserInputService = game:GetService("UserInputService")
local dragging = false
local dragStart = Vector2.new(0, 0)
local startPos = frame.Position

frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
    end
end)

frame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- 标题
local title = Instance.new("TextLabel")
title.Text = "Boss监控系统 v3.0"
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Parent = frame

-- 区域选择
local buttonsFrame = Instance.new("Frame")
buttonsFrame.Size = UDim2.new(1, -20, 0, 150)
buttonsFrame.Position = UDim2.new(0, 10, 0, 40)
buttonsFrame.BackgroundTransparency = 1
buttonsFrame.Parent = frame

local areaConfig = {
    [100] = false, [101] = false, [102] = false,
    [103] = false, [104] = false, [105] = false, 
    [106] = false
}

for i, areaId in ipairs({100, 101, 102, 103, 104, 105, 106}) do
    local col = (i-1) % 2
    local row = math.floor((i-1) / 2)
    
    local button = Instance.new("TextButton")
    button.Text = string.format("区域 %d: 关闭", areaId)
    button.Size = UDim2.new(0.45, 0, 0, 30)
    button.Position = UDim2.new(col * 0.55, 0, row * 0.1, row * 30)
    button.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    
    button.MouseButton1Click:Connect(function()
        areaConfig[areaId] = not areaConfig[areaId]
        button.Text = string.format("区域 %d: %s", areaId, areaConfig[areaId] and "开启" or "关闭")
        button.BackgroundColor3 = areaConfig[areaId] and Color3.fromRGB(30, 80, 30) or Color3.fromRGB(80, 30, 30)
    end)
    
    button.Parent = buttonsFrame
end

-- 功能开关
local lingShouToggle = Instance.new("TextButton")
lingShouToggle.Text = "灵兽监控: 关闭"
lingShouToggle.Size = UDim2.new(0.8, 0, 0, 30)
lingShouToggle.Position = UDim2.new(0.1, 0, 0, 200)
lingShouToggle.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
lingShouToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
lingShouToggle.Parent = frame

local lingShouStatus = false
lingShouToggle.MouseButton1Click:Connect(function()
    lingShouStatus = not lingShouStatus
    lingShouToggle.Text = "灵兽监控: " .. (lingShouStatus and "开启" or "关闭")
    lingShouToggle.BackgroundColor3 = lingShouStatus and Color3.fromRGB(30, 80, 30) or Color3.fromRGB(80, 30, 30)
end)

-- 停止按钮
local stopButton = Instance.new("TextButton")
stopButton.Text = "停止所有监控"
stopButton.Size = UDim2.new(0.8, 0, 0, 30)
stopButton.Position = UDim2.new(0.1, 0, 0, 240)
stopButton.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
stopButton.TextColor3 = Color3.fromRGB(255, 255, 255)
stopButton.Parent = frame

gui.Parent = player:WaitForChild("PlayerGui")

-- ========== 核心功能 ==========
local running = true
local currentFloatTask = nil
local currentCircleTask = nil
local currentCenterPosition = nil
local hasEnteredBattle = false
local targetPosition = Vector3.new(-70, 18, -62)

-- 安全获取对象
local function safeGet(path)
    local current = game
    for _, part in ipairs(path) do
        local success, child = pcall(function() return current[part] end)
        if not success or not child then
            return nil
        end
        current = child
    end
    return current
end

-- 战斗状态检测
local function isWaitClearVisible()
    local waitFrame = safeGet({
        "Players", player.Name, 
        "PlayerGui", "MainGui", 
        "ScreenGui", "ArenaStatusView", 
        "ResultFrame"
    })
    return waitFrame and waitFrame:IsA("Frame") and waitFrame.Visible
end

local function isInBattle()
    local arenaView = safeGet({
        "Players", player.Name,
        "PlayerGui", "MainGui",
        "ScreenGui", "ArenaStatusView"
    })
    return arenaView and arenaView:IsA("Frame")
end

-- 获取视角前方20单位的位置
local function getViewForwardCenter()
    local Character = player.Character
    if not Character then return nil end
    
    local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")
    if not HumanoidRootPart then return nil end

    local camera = workspace.CurrentCamera
    if camera then
        local cameraCF = camera.CFrame
        return Vector3.new(
            cameraCF.Position.X + cameraCF.LookVector.X * 100,
            HumanoidRootPart.Position.Y,
            cameraCF.Position.Z + cameraCF.LookVector.Z * 100
        )
    end
    return HumanoidRootPart.Position
end

-- 圆形移动（半径30）
local function startCircleMovement()
    if currentCircleTask then
        currentCircleTask:Disconnect()
        currentCircleTask = nil
    end

    local Character = player.Character
    if not Character then return end
    
    local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")
    if not HumanoidRootPart then return end

    local radius = 60
    local angle = 0
    local speed = 0.013
    
    currentCircleTask = game:GetService("RunService").Heartbeat:Connect(function(dt)
        if not running or not currentCenterPosition or not HumanoidRootPart or not HumanoidRootPart.Parent then
            if currentCircleTask then
                currentCircleTask:Disconnect()
                currentCircleTask = nil
            end
            return
        end

        angle = angle + speed * dt * 60
        local x = currentCenterPosition.X + math.cos(angle) * radius
        local z = currentCenterPosition.Z + math.sin(angle) * radius
        
        HumanoidRootPart.CFrame = CFrame.new(
            Vector3.new(x, HumanoidRootPart.Position.Y, z),
            currentCenterPosition
        )
    end)
end

-- 浮空控制
local function floatAbovePosition()
    -- 清理现有状态
    if currentFloatTask then
        currentFloatTask:Disconnect()
        currentFloatTask = nil
    end
    if currentCircleTask then
        currentCircleTask:Disconnect()
        currentCircleTask = nil
    end
    currentCenterPosition = nil

    local Character = player.Character or player.CharacterAdded:Wait()
    local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
    local Humanoid = Character:WaitForChild("Humanoid")

    -- 清理旧控制器
    for _, child in ipairs(HumanoidRootPart:GetChildren()) do
        if child:IsA("BodyPosition") or child:IsA("BodyForce") then
            child:Destroy()
        end
    end

    -- 创建浮空控制器
    local bodyPos = Instance.new("BodyPosition")
    bodyPos.Position = HumanoidRootPart.Position + Vector3.new(0, 25, 0)
    bodyPos.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyPos.D = 1000
    bodyPos.P = 10000
    bodyPos.Parent = HumanoidRootPart

    local bodyForce = Instance.new("BodyForce")
    bodyForce.Force = Vector3.new(0, HumanoidRootPart:GetMass() * workspace.Gravity, 0)
    bodyForce.Parent = HumanoidRootPart

    Humanoid:ChangeState(Enum.HumanoidStateType.Physics)

    -- 等待浮空稳定
    task.wait(0.5)
    
    -- 设置中心点并开始圆形移动
    currentCenterPosition = getViewForwardCenter()
    if currentCenterPosition then
        startCircleMovement()
    else
        warn("无法获取中心点位置")
    end

    -- 战斗结束检测
    currentFloatTask = game:GetService("RunService").Heartbeat:Connect(function()
        if not running then
            if currentFloatTask then
                currentFloatTask:Disconnect()
                currentFloatTask = nil
            end
            return
        end

        if isWaitClearVisible() then
            print("战斗结束，停止所有效果")
            -- 清理所有状态
            if currentFloatTask then
                currentFloatTask:Disconnect()
                currentFloatTask = nil
            end
            if currentCircleTask then
                currentCircleTask:Disconnect()
                currentCircleTask = nil
            end
            currentCenterPosition = nil
            
            pcall(function()
                bodyPos:Destroy()
                bodyForce:Destroy()
                Humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end)
        end
    end)
end

-- Boss区域检测
local function checkBossArea(areaId)
    local guiAth = safeGet({
        "Workspace", "AreaModel", "101", 
        "Area", "CommonGui", 
        "ArenaSoloShower_"..areaId, "GuiAth"
    })
    
    if not guiAth then
        warn("["..areaId.."] 路径获取失败")
        return false
    end

    local function searchLabCd(instance)
        if instance:IsA("TextLabel") and instance.Name == "LabCdTime" then
            local text = instance.Text
            return text == "LabCdTime" or text == "CD: 00:00:00"
        end
        for _, child in ipairs(instance:GetChildren()) do
            if searchLabCd(child) then return true end
        end
        return false
    end

    return searchLabCd(guiAth)
end

-- 主循环
local function main()
    while running and gui.Parent do
        for areaId, enabled in pairs(areaConfig) do
            if enabled and not isInBattle() then
                local success, ready = pcall(checkBossArea, areaId)
                if success and ready then
                    print("["..areaId.."] 可进入战斗")
                    
                    local args = {"ArenaStartSoloArenaChannel", areaId}
                    local invokeSuccess, err = pcall(function()
                        game:GetService("ReplicatedStorage").CommonLibrary.Tool.RemoteManager.Funcs.DataPullFunc:InvokeServer(unpack(args))
                    end)
                    
                    if invokeSuccess then
                        print("战斗请求已发送")
                        hasEnteredBattle = true
                        
                        local startTime = os.time()
                        local hasTriggered = false
                        
                        while running and (os.time() - startTime < 15) do
                            if isInBattle() and not hasTriggered then
                                print("进入战斗，启动浮空系统")
                                floatAbovePosition()
                                hasTriggered = true
                                break
                            end
                            task.wait(0.3)
                        end
                    else
                        warn("战斗请求失败:", err)
                    end
                end
            end
        end

        if hasEnteredBattle and not isInBattle() then
            local character = player.Character
            if character then
                local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                if humanoidRootPart then
                    print("战斗结束，传送到目标位置")
                    task.wait(2)
                    humanoidRootPart.CFrame = CFrame.new(targetPosition)
                    hasEnteredBattle = false
                end
            end
        end

        task.wait(1)
    end
end

-- 停止功能
stopButton.MouseButton1Click:Connect(function()
    running = false
    if currentFloatTask then
        currentFloatTask:Disconnect()
        currentFloatTask = nil
    end
    if currentCircleTask then
        currentCircleTask:Disconnect()
        currentCircleTask = nil
    end
    currentCenterPosition = nil
    gui:Destroy()
end)

-- 启动脚本
local success, err = pcall(main)
if not success then
    warn("脚本错误:", err)
    gui:Destroy()
end
